OPENROUTER_API_KEY=sk-or-v1-change-me

# Optional: pin the Jev model used by the `jev` rule.
# The default alias moves between releases; pin a version when tuning thresholds.
OPENROUTER_DECISIONS_MODEL=~typesafe/jev-latest
